from sqlalchemy import text

async def hybrid_search(db_session, query_text, query_vector, limit=5):
    """
    Тут будет твой код из Kaggle (RRF + Vector Search).
    Пока делаем заглушку для теста.
    """
    return []
